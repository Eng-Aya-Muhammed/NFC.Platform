namespace NFC.Platform.Domain.Constants
{
    public static class SubscriptionConstants
    {
        /// <summary>
        /// Represents an unlimited quota for subscription limits (e.g., max employees, max templates).
        /// </summary>
        public const int UnlimitedQuota = -1;
    }
}
